adjust fonts to use scss variables
use rem over px

On big screen - widen TOC by 20/30 px, scrolling bar on chrome issue

On mobile screen - font sizes (make everything em/rem)

On non mobile screens - scrolling of TOC should jump by more than 1 header

smaller file sizes, better hierarchy

finish maximal ideals content

start with some prelimary small standalone content for other topics, then aggregate over time?