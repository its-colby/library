import { COMMUTATIVITY } from "./commutativity";
import { ASSOCIATIVITY } from "./associativity";
import { DISTRIBUTIVITY } from "./distributivity";
import { TWO_SIDED_IDENTITY } from "./two_sided_identity";
import { EXISTENCE_OF_INVERSES } from "./existence_of_inverses";

export const PROPERTIES = {
    COMMUTATIVITY,
    ASSOCIATIVITY,
    DISTRIBUTIVITY,
    TWO_SIDED_IDENTITY,
    EXISTENCE_OF_INVERSES
} as const;
