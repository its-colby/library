import { Title, Section } from "$lib/tex";
import { INTRODUCTION } from "./01-introduction";

export const QUADRATIC_FIELDS = new Section({
    title: new Title({
        value: "Quadratic Fields",
    }),
    sections: [
        INTRODUCTION,
    ]
})
