import { Title, Section } from "$lib/tex";
import { PROOF } from "./proof";

export const GAUSS_LEMMA = new Section({
    title: new Title({
        value: "Gauss's Lemma",
    }),
    sections: [
        PROOF,
    ]
})
